name: ui
publish_to: none

# Keep the SDK version range in sync with pubspecs under web_sdk
environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  js: 0.6.3
  meta: 1.3.0

dev_dependencies:
  html: 0.15.0
  quiver: 3.0.0
  test: 1.17.7
  web_engine_tester:
    path: ../../web_sdk/web_engine_tester
